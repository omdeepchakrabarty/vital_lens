version: "3.9"
services:
  vital-lens:
    build: .
    ports:
      - "8000:8000"
    # Production-safe defaults: no host bind mounts, no autoreload.
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
